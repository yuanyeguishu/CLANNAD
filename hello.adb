with Ada.Text_IO; -- 表示使用这个package的内容  
use Ada.Text_IO;  -- 表示展开这个package的命名空间  
procedure Hello is  -- 定义过程，注意这个is就显得很严谨  
begin  
Put_Line("云之彼端 约定的地方");   -- 打印  
end Hello;        -- end后必须跟名称  